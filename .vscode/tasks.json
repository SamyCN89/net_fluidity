{
  "version": "2.0.0",
  "tasks": [
    { "label": "pytest (all)", "type": "shell", "command": "pytest -q", "group": "test", "problemMatcher": "$pytest" },
    { "label": "ruff check",   "type": "shell", "command": "ruff check ." },
    { "label": "black check",  "type": "shell", "command": "black --check ." }
  ]
}
